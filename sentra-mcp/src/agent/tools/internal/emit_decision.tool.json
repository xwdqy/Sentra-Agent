{
  "type": "function",
  "function": {
    "name": "emit_decision",
    "description": "判断是否需要调用外部工具来完成目标。返回结构化的判断结果：need_tools（布尔值）、summary（需求总结）、operations（操作列表数组）。",
    "parameters": {
      "$ref": "./emit_decision.schema.json"
    }
  }
}
