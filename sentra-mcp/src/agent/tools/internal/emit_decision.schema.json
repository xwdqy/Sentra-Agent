{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "need_tools": {
      "type": "boolean",
      "description": "Optional compatibility field. Usually omitted in tool-gate mode."
    },
    "tool_names": {
      "type": "array",
      "description": "Compact shortlist of selected tool aiNames. Output only names from the provided catalog. Do not generate arguments here.",
      "uniqueItems": true,
      "items": {
        "type": "string"
      },
      "maxItems": 12,
      "examples": [
        ["local__read_file"],
        ["local__search_web", "local__read_url_content"],
        []
      ]
    }
  },
  "required": ["tool_names"],
  "additionalProperties": true
}
