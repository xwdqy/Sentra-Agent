{
  "zh": "# Sentra XML 协议完整说明\n\n本系统使用 Sentra XML 协议作为统一的结构化数据表示格式。所有工具调用、执行结果和用户问题都使用此协议。\n\n## 协议标签说明\n\n### 1. <sentra-user-question> - 用户原始问题\n用于标记用户的原始输入问题或需求。\n\n格式：\n```xml\n<sentra-user-question>用户的原始问题文本</sentra-user-question>\n```\n\n示例：\n```xml\n<sentra-user-question>OpenAI最近有什么动态？</sentra-user-question>\n```\n\n### 2. <sentra-tools> - 工具调用\n用于调用外部工具或函数。这是你主动发起的操作。\n\n格式：\n```xml\n<sentra-tools>\n  <invoke name=\"工具名称\">\n    <parameter name=\"参数1\">值1</parameter>\n    <parameter name=\"参数2\">{\"key\": \"value\"}</parameter>\n  </invoke>\n</sentra-tools>\n```\n\n参数值规则：\n- String/scalar（字符串/标量）：直接指定，无需 JSON 转义\n- Objects/arrays（对象/数组）：使用 JSON 格式\n- 数字：直接写数字（如 `42`）\n- 布尔：直接写 `true` 或 `false`\n- 字符串值中的空格会被保留\n\n示例：\n```xml\n<sentra-tools>\n  <invoke name=\"search_web\">\n    <parameter name=\"query\">OpenAI 最新动态</parameter>\n    <parameter name=\"max_results\">10</parameter>\n  </invoke>\n</sentra-tools>\n```\n\n### 3. <sentra-result> - 工具执行结果\n用于展示工具执行后的观测结果。这是系统返回给你的信息，你不能伪造。\n\n格式：\n```xml\n<sentra-result step=\"步骤索引\" tool=\"工具名称\" success=\"true|false\">\n  <reason>使用此工具的原因</reason>\n  <args>{\"arg1\": \"value1\"}</args>\n  <data>执行结果数据（JSON格式）</data>\n</sentra-result>\n```\n\n属性说明：\n- `step`：步骤索引（数字）\n- `tool`：工具名称\n- `success`：执行是否成功（true/false）\n\n子标签说明：\n- `<reason>`：使用此工具的原因或目的\n- `<args>`：实际调用时使用的参数（JSON格式）\n- `<data>`：工具返回的数据（JSON格式）\n\n示例：\n```xml\n<sentra-result step=\"0\" tool=\"search_web\" success=\"true\">\n  <reason>搜索信息; 获取最新动态</reason>\n  <args>{\"query\": \"OpenAI 最新动态\", \"max_results\": 10}</args>\n  <data>[{\"title\": \"OpenAI发布GPT-5\", \"url\": \"https://...\", \"snippet\": \"...\"}]</data>\n</sentra-result>\n```\n\n## 使用原则\n\n1. **只读性**：`<sentra-result>` 标签由系统生成，你只能阅读和引用，禁止伪造或修改。\n\n2. **一致性**：所有工具调用必须使用 `<sentra-tools>` 标签，确保格式统一。\n\n3. **可追溯性**：每个结果都包含 `step` 属性，可以追溯到具体的执行步骤。\n\n4. **语义明确**：使用 XML 标签而非纯 JSON，使得语义更清晰，易于理解和调试。\n\n5. **上下文传递**：在多步骤任务中，前序步骤的 `<sentra-result>` 会作为上下文传递给后续步骤。\n\n## 完整示例\n\n用户请求：\n```xml\n<sentra-user-question>帮我搜索最新的AI新闻，然后总结成中文</sentra-user-question>\n```\n\n步骤1 - 搜索：\n```xml\n<sentra-tools>\n  <invoke name=\"search_web\">\n    <parameter name=\"query\">最新AI新闻</parameter>\n    <parameter name=\"max_results\">5</parameter>\n  </invoke>\n</sentra-tools>\n```\n\n步骤1 - 结果：\n```xml\n<sentra-result step=\"0\" tool=\"search_web\" success=\"true\">\n  <reason>搜索最新AI新闻</reason>\n  <args>{\"query\": \"最新AI新闻\", \"max_results\": 5}</args>\n  <data>[{\"title\": \"GPT-5发布\", \"snippet\": \"...\"}, ...]</data>\n</sentra-result>\n```\n\n步骤2 - 总结（你可以在这里引用步骤0的结果）：\n基于 `<sentra-result step=\"0\">` 的数据，生成总结...\n\n## 注意事项\n\n- 禁止使用 Markdown 代码块围栏（```）包裹 XML 标签\n- 禁止输出多个 `<sentra-tools>` 块\n- 参数名必须与 schema 完全一致（包括大小写）\n- JSON 数据需要正确转义特殊字符\n- 保持 XML 格式的正确性（标签闭合、属性引号等）",
  "en": "# Sentra XML Protocol Complete Specification\n\nThis system uses the Sentra XML Protocol as a unified structured data representation format. All tool invocations, execution results, and user questions use this protocol.\n\n## Protocol Tags\n\n### 1. <sentra-user-question> - User Original Question\nUsed to mark the user's original input question or requirement.\n\nFormat:\n```xml\n<sentra-user-question>User's original question text</sentra-user-question>\n```\n\nExample:\n```xml\n<sentra-user-question>What are the latest updates from OpenAI?</sentra-user-question>\n```\n\n### 2. <sentra-tools> - Tool Invocation\nUsed to call external tools or functions. This is an operation you initiate.\n\nFormat:\n```xml\n<sentra-tools>\n  <invoke name=\"tool_name\">\n    <parameter name=\"param1\">value1</parameter>\n    <parameter name=\"param2\">{\"key\": \"value\"}</parameter>\n  </invoke>\n</sentra-tools>\n```\n\nParameter value rules:\n- String/scalar: specify directly, no JSON escaping needed\n- Objects/arrays: use JSON format\n- Numbers: write directly (e.g., `42`)\n- Booleans: write `true` or `false` directly\n- Spaces in string values are preserved\n\nExample:\n```xml\n<sentra-tools>\n  <invoke name=\"search_web\">\n    <parameter name=\"query\">OpenAI latest news</parameter>\n    <parameter name=\"max_results\">10</parameter>\n  </invoke>\n</sentra-tools>\n```\n\n### 3. <sentra-result> - Tool Execution Result\nUsed to display observed results after tool execution. This is information returned by the system; you cannot forge it.\n\nFormat:\n```xml\n<sentra-result step=\"step_index\" tool=\"tool_name\" success=\"true|false\">\n  <reason>Reason for using this tool</reason>\n  <args>{\"arg1\": \"value1\"}</args>\n  <data>Execution result data (JSON format)</data>\n</sentra-result>\n```\n\nAttribute description:\n- `step`: Step index (number)\n- `tool`: Tool name\n- `success`: Whether execution succeeded (true/false)\n\nSub-tag description:\n- `<reason>`: Reason or purpose for using this tool\n- `<args>`: Actual parameters used during invocation (JSON format)\n- `<data>`: Data returned by the tool (JSON format)\n\nExample:\n```xml\n<sentra-result step=\"0\" tool=\"search_web\" success=\"true\">\n  <reason>Search for information; Get latest updates</reason>\n  <args>{\"query\": \"OpenAI latest news\", \"max_results\": 10}</args>\n  <data>[{\"title\": \"OpenAI releases GPT-5\", \"url\": \"https://...\", \"snippet\": \"...\"}]</data>\n</sentra-result>\n```\n\n## Usage Principles\n\n1. **Read-only**: `<sentra-result>` tags are system-generated; you can only read and reference them, not forge or modify.\n\n2. **Consistency**: All tool calls must use `<sentra-tools>` tags to ensure format uniformity.\n\n3. **Traceability**: Each result includes a `step` attribute for tracing to specific execution steps.\n\n4. **Semantic clarity**: Using XML tags instead of pure JSON makes semantics clearer and easier to understand and debug.\n\n5. **Context passing**: In multi-step tasks, `<sentra-result>` from previous steps is passed as context to subsequent steps.\n\n## Complete Example\n\nUser request:\n```xml\n<sentra-user-question>Search for the latest AI news and summarize it in Chinese</sentra-user-question>\n```\n\nStep 1 - Search:\n```xml\n<sentra-tools>\n  <invoke name=\"search_web\">\n    <parameter name=\"query\">latest AI news</parameter>\n    <parameter name=\"max_results\">5</parameter>\n  </invoke>\n</sentra-tools>\n```\n\nStep 1 - Result:\n```xml\n<sentra-result step=\"0\" tool=\"search_web\" success=\"true\">\n  <reason>Search for latest AI news</reason>\n  <args>{\"query\": \"latest AI news\", \"max_results\": 5}</args>\n  <data>[{\"title\": \"GPT-5 released\", \"snippet\": \"...\"}, ...]</data>\n</sentra-result>\n```\n\nStep 2 - Summarize (you can reference step 0 results here):\nBased on data from `<sentra-result step=\"0\">`, generate summary...\n\n## Important Notes\n\n- Do NOT use Markdown code block fences (```) to wrap XML tags\n- Do NOT output multiple `<sentra-tools>` blocks\n- Parameter names must exactly match the schema (including case)\n- JSON data needs proper escaping for special characters\n- Maintain correct XML format (tag closure, attribute quotes, etc.)"
}
