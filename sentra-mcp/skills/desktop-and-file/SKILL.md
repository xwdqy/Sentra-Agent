# 桌面 / 系统 / 文件（Desktop & File）

## 适用场景
- 需要读取系统信息、进行桌面操作、或把内容写入本地文件。

## 工作流
1. 先确认目标
   - 路径/文件名/目标窗口等必须明确。
2. 再执行
   - 系统信息：`local__system_info`
   - 桌面控制：`local__desktop_control`
   - 写文件：`local__write_file`

## 参数/约束
- 写文件路径必须明确且可写；如果用户未指定路径，优先写到项目目录下的明确子目录。
- 桌面控制属于真实动作，避免多次重复执行。
