{
  "name": "av_transcribe",
  "description": "音视频转文本：支持多种音视频格式（mp3/mp4/wav/amr等），输出转录文本和语言",
  "inputSchema": {
    "type": "object",
    "properties": {
      "file": {
        "type": "string",
        "description": "音视频文件路径（必填）。支持：1) **本地绝对路径**（必须包含完整盘符或根目录，不能使用相对路径）；2) http/https URL。支持格式：mp3/mp4/wav/amr/m4a/webm/flac/ogg 等。",
        "examples": [
          "E:/media/audio.mp3",
          "C:/Users/Admin/Videos/video.mp4",
          "/home/user/music/song.wav",
          "/var/media/recording.m4a",
          "https://example.com/audio.mp3"
        ]
      },
      "files": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "音视频文件路径列表（批量模式，必填其一：file/files）。每个元素规则同 file：支持本地绝对路径或 http/https URL。",
        "examples": [
          [
            "E:/media/audio1.mp3",
            "E:/media/audio2.mp3"
          ]
        ]
      },
      "language": {
        "type": "string",
        "description": "音频语言（可选）：如 zh/en/ja/ko 等。不指定则自动检测"
      },
      "prompt": {
        "type": "string",
        "description": "提示文本（可选）：引导识别风格、纠正特定词汇"
      },
      "schedule": {
        "type": "object",
        "properties": {
          "when": {
            "type": "string",
            "description": "时间表达：如 '5分钟后' / '30秒后' / '明天09:00' / '2025-12-16 21:30' / ISO 字符串。请使用清晰、可解析的时间表达，避免使用『稍后』『改天』『有空的时候』『尽快』『5分钟之后』等模糊说法。"
          },
          "language": {
            "type": "string",
            "description": "语言标识，例如 zh|en（默认 zh）"
          },
          "timezone": {
            "type": "string",
            "description": "IANA 时区名称，例如 Asia/Shanghai"
          },
          "targetISO": {
            "type": "string",
            "description": "预解析好的绝对时间（ISO 字符串），例如 '2025-12-16T21:30:00+08:00'；如提供，将被上游优先用作目标时间。"
          }
        },
        "required": [
          "when",
          "targetISO"
        ],
        "additionalProperties": true,
        "description": "可选：当用户需要延迟处理或延迟反馈时使用。具体是仅延迟反馈还是到点再执行，由上层调度策略（例如 immediate_exec / delayed_exec）决定；若到点未完成，将先反馈进行中状态。"
      }
    },
    "anyOf": [
      {
        "required": [
          "file"
        ]
      },
      {
        "required": [
          "files"
        ]
      }
    ]
  },
  "timeoutMs": 180000,
  "meta": {
    "realWorldAction": "音视频转文本",
    "responseStyle": "纯文本",
    "responseExample": "{ success: true, data: { text: '完整转录文本', segments: [{ index: 0, text: '第一段' }, { index: 1, text: '第二段' }], language: { raw: '中文 / Chinese', code: 'zh', name: 'Chinese', hint: 'zh' }, meta: { model: 'whisper-1', file: 'E:/media/audio.mp3', chunks: 1, source: 'file' } } }"
  }
}
