{
  "name": "video_vision_read",
  "description": "读取并分析一个或多个视频。支持视频在线链接（http/https）或本地绝对路径；结合文本提示进行视频内容识别并返回分析结果。支持常见视频格式包括 MP4、MOV、AVI、WebM、MKV 等。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "video": {
        "type": "string",
        "description": "单个视频路径（字符串）。支持：1) 本地绝对路径；2) http/https URL。若需要一次处理多个视频，请使用 videos[]。"
      },
      "videos": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "minItems": 1,
        "description": "视频路径数组。支持：1) **本地绝对路径**（必须包含完整盘符或根目录）；2) http/https URL。禁止使用相对路径或仅文件名。支持格式：MP4/MOV/AVI/WebM/MKV 等。",
        "examples": [
          [
            "E:/videos/demo.mp4",
            "C:/Users/Admin/Videos/clip.avi"
          ],
          [
            "/home/user/videos/movie.mp4",
            "/var/media/presentation.webm"
          ],
          [
            "https://example.com/video.mp4"
          ]
        ]
      },
      "prompt": {
        "type": "string",
        "description": "Instruction or question for the videos. For example: 'Describe what happens in this video', 'Extract text from this video', 'What actions are performed in this video?'"
      }
    },
    "required": [
      "prompt"
    ],
    "anyOf": [
      {
        "required": [
          "video"
        ]
      },
      {
        "required": [
          "videos"
        ]
      }
    ],
    "additionalProperties": true
  },
  "timeoutMs": 180000,
  "meta": {
    "streamSupport": true
  },
  "trigger_keywords": [
    "视频理解",
    "视频解析",
    "video analysis",
    "video caption"
  ],
  "trigger_patterns": [
    "(视频理解|视频解析).{0,12}(video analysis|video caption)",
    "(video analysis|video caption).{0,12}(视频理解|视频解析)",
    "(视频理解|视频解析|video analysis|video caption)"
  ],
  "keyword_boost": 65,
  "regex_boost": 90
}
