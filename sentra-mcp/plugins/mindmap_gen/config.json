{
  "name": "mindmap_gen",
  "description": "Generate a detailed mind map from structured prompt data. Accepts comprehensive information including central topic, main branches, sub-branches, relationships, and styling preferences. Returns both markdown representation and optionally renders a high-quality PNG image via Puppeteer",
  "inputSchema": {
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string"
      },
      "width": {
        "type": "integer",
        "minimum": 400,
        "default": 2400
      },
      "height": {
        "type": "integer",
        "minimum": 300,
        "default": 1600
      },
      "style": {
        "type": "string",
        "default": "default"
      },
      "render": {
        "type": "boolean",
        "default": true
      },
      "waitTime": {
        "type": "integer",
        "minimum": 1000,
        "default": 8000
      },
      "filename": {
        "type": "string",
        "description": "输出文件名（仅文件名，不含目录）。保存目录固定为 artifacts/。建议包含时间戳以避免重名，例如：topic_1696231234567.png。插件不会自动追加时间戳，需调用方提供。"
      }
    },
    "required": [
      "prompt",
      "filename"
    ],
    "additionalProperties": true
  },
  "timeoutMs": 150000,
  "trigger_keywords": [
    "思维导图",
    "脑图",
    "mindmap",
    "mind map",
    "大纲转导图"
  ],
  "trigger_patterns": [
    "(思维导图|脑图).{0,12}(mindmap|mind map|大纲转导图)",
    "(mindmap|mind map|大纲转导图).{0,12}(思维导图|脑图)",
    "(思维导图|脑图|mindmap|mind map|大纲转导图)"
  ],
  "keyword_boost": 63,
  "regex_boost": 88
}
