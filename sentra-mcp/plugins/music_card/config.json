{
  "name": "music_card",
  "description": "根据关键词搜索网易云音乐并通过 WebSocket SDK 发送音乐卡片到 QQ 私聊或群聊",
  "inputSchema": {
    "type": "object",
    "properties": {
      "keyword": {
        "type": "string",
        "description": "单个音乐标题或关键词（字符串）。若需要一次发送多个关键词，请使用 keywords[]。"
      },
      "keywords": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "minItems": 1,
        "description": "音乐标题或关键词数组，每项可为 歌曲名 或 歌曲名+歌手名，例如：[\"稻香 周杰伦\", \"夜曲 周杰伦\"]"
      },
      "provider": {
        "type": "string",
        "default": "163",
        "description": "音乐卡片提供方，默认 163（网易云）"
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 10,
        "default": 6,
        "description": "搜索结果返回数量上限（1-10）"
      },
      "random": {
        "type": "boolean",
        "default": true,
        "description": "是否从结果中随机挑选一首"
      },
      "pick": {
        "type": "integer",
        "minimum": 0,
        "maximum": 9,
        "description": "在结果集中选择索引（当 random=false 时有效）"
      },
      "user_id": {
        "type": "string",
        "description": "私聊对象的 user_id（可选，同时提供时优先使用 group_id）"
      },
      "group_id": {
        "type": "string",
        "description": "群聊对象的 group_id（可选，同时提供时优先使用此参数）"
      },
      "fallback_to_record": {
        "type": "boolean",
        "default": false,
        "description": "发送音乐卡片失败时，尝试发送音频直链（需要可用的播放链接）"
      },
      "schedule": {
        "type": "object",
        "properties": {
          "when": {
            "type": "string",
            "description": "时间表达：如 '5分钟后' / '30秒后' / '明天09:00' / '2025-12-16 21:30' / ISO 字符串。请使用清晰、可解析的时间表达，避免使用『稍后』『改天』『有空的时候』『尽快』『5分钟之后』等模糊说法。"
          },
          "language": {
            "type": "string",
            "description": "语言标识，例如 zh|en（默认 zh）"
          },
          "timezone": {
            "type": "string",
            "description": "IANA 时区名称，例如 Asia/Shanghai"
          },
          "targetISO": {
            "type": "string",
            "description": "预解析好的绝对时间（ISO 字符串），例如 '2025-12-16T21:30:00+08:00'；如提供，将被上游优先用作目标时间。"
          }
        },
        "required": [
          "when",
          "targetISO"
        ],
        "additionalProperties": true,
        "description": "可选：当用户需要延迟处理或延迟反馈时使用。具体是仅延迟反馈还是到点再执行，由上层调度策略（例如 immediate_exec / delayed_exec）决定；若到点未完成，将先反馈进行中状态。"
      }
    },
    "required": [],
    "anyOf": [
      { "required": ["keyword"] },
      { "required": ["keywords"] }
    ],
    "additionalProperties": true
  },
  "timeoutMs": 40000,
  "meta": {
    "realWorldAction": "搜索并发送音乐卡片",
    "responseStyle": "结构化 JSON（中文键）"
  }
}
