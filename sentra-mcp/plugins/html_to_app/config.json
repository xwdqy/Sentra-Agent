{
  "name": "html_to_app",
  "description": "将 HTML 代码或应用需求描述转换为完整的桌面应用项目（基于 Electron），支持原生 HTML、React、Vue 等框架。自动生成项目结构、依赖配置、打包脚本，可直接运行和打包为 exe/dmg/AppImage。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "description": {
        "type": "string",
        "description": "应用描述或需求（必填）。可以是功能描述、UI 要求、业务逻辑等，插件会根据描述生成完整应用代码。"
      },
      "app_name": {
        "type": "string",
        "description": "应用名称（英文，用于生成项目文件夹和 package.json）"
      },
      "details": {
        "type": "string",
        "description": "细节补充要求（必填）。具体的 UI/UX 细节，如：背景图片、颜色方案、字体、图标、动画效果、布局细节等。越详细越好。"
      },
      "html_content": {
        "type": "string",
        "description": "可选：已有的 HTML 代码。如果提供，将作为应用的主要界面。"
      },
      "framework": {
        "type": "string",
        "description": "可选：前端框架 - vanilla(原生HTML) | react(React+Vite) | vue(Vue3+Vite)，默认 vanilla"
      },
      "features": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "可选：需要的功能特性，如：['文件读写', '系统托盘', '自动更新', '数据库']"
      }
    },
    "required": [
      "description",
      "app_name",
      "details"
    ],
    "additionalProperties": true
  },
  "timeoutMs": 600000,
  "meta": {
    "streamSupport": true
  },
  "trigger_keywords": [
    "html转app",
    "h5转app",
    "封装app",
    "webview",
    "html to app",
    "pack app"
  ],
  "trigger_patterns": [
    "(html转app|h5转app).{0,12}(封装app|webview|html to app|pack app)",
    "(封装app|webview|html to app|pack app).{0,12}(html转app|h5转app)",
    "(html转app|h5转app|封装app|webview|html to app|pack app)"
  ],
  "keyword_boost": 61,
  "regex_boost": 86
}
