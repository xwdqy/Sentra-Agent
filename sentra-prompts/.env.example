# 获取当前时间（HH:mm:ss 格式）
# type: string
time=getCurrentTime

# 获取当前日期（YYYY-MM-DD 格式）
# type: string
date=getCurrentDate

# 获取星期几（中文）
# type: string
weekday=getWeekday

# 获取 ISO 8601 格式时间
# type: string
iso_time=getISOTime

# 获取时区信息
# type: string
timezone=getTimezone

# 获取 Unix 时间戳（毫秒）
# type: string
timestamp=getTimestamp

# 获取当前年份
# type: string
year=getYear

# 获取当前月份
# type: string
month=getMonth

# 获取当前日期（天）
# type: string
day=getDay

# 获取问候语（早上好/下午好/晚上好）
# type: string
greeting=getGreeting

# 获取当前小时（0-23）
# type: string
current_hour=getCurrentHour

# 获取节假日信息
# type: string
holiday=getHolidayInfo

# 获取农历日期
# type: string
lunar_date=getLunarDate

# 获取生肖
# type: string
zodiac=getZodiac

# 获取天干地支
# type: string
ganzi=getGanZhi

# 判断是否为工作日
# type: string
is_workday=isWorkday

# 判断是否为周末
# type: string
is_weekend=isWeekend

# 获取下一个节假日信息
# type: string
next_holiday=getNextHoliday

# 获取当前节气
# type: string
jieqi=getCurrentJieQi

# 获取星座
# type: string
constellation=getConstellation

# 获取当前季节
# type: string
season=getCurrentSeason

# 获取当前是一年中的第几周
# type: string
week_of_year=getWeekOfYear

# 获取当前是一年中的第几天
# type: string
day_of_year=getDayOfYear

# 获取当月剩余天数
# type: string
remaining_days_month=getRemainingDaysInMonth

# 获取当年剩余天数
# type: string
remaining_days_year=getRemainingDaysInYear

# 判断是否为月初（1-3 号）
# type: string
is_month_start=isMonthStart

# 判断是否为月末（最后 3 天）
# type: string
is_month_end=isMonthEnd

# 判断是否为年初（1 月）
# type: string
is_year_start=isYearStart

# 判断是否为年末（12 月）
# type: string
is_year_end=isYearEnd

# 获取距离周末的天数
# type: string
days_until_weekend=getDaysUntilWeekend

# 获取距离下周一的天数
# type: string
days_until_monday=getDaysUntilMonday

# 获取当月总天数
# type: string
days_in_month=getDaysInMonth

# 判断是否为闰年
# type: string
is_leap_year=isLeapYear

# 获取昨天日期
# type: string
yesterday=getYesterday

# 获取明天日期
# type: string
tomorrow=getTomorrow

# 获取上个月信息
# type: string
last_month=getLastMonth

# 获取下个月信息
# type: string
next_month=getNextMonth

# 获取当前季度（Q1-Q4）
# type: string
quarter=getCurrentQuarter

# 获取距离年底的天数
# type: string
days_until_year_end=getDaysUntilYearEnd

# 获取星期几（数字 0-6）
# type: string
day_of_week=getDayOfWeek

# 判断是否为工作时间（9:00-18:00）
# type: string
is_working_hours=isWorkingHours

# 判断是否为休息时间
# type: string
is_rest_time=isRestTime

# 获取年度进度百分比
# type: string
year_progress=getYearProgress

# 获取月度进度百分比
# type: string
month_progress=getMonthProgress

# 获取当天进度百分比
# type: string
day_progress=getDayProgress

# 获取当月第一天日期
# type: string
first_day_of_month=getFirstDayOfMonth

# 获取当月最后一天日期
# type: string
last_day_of_month=getLastDayOfMonth

# 格式化日期为 YYYY-MM-DD
# type: string
date_ymd=formatDateYMD

# 格式化时间为 24 小时制
# type: string
time_24=formatTime24

# 格式化时间为 12 小时制
# type: string
time_12=formatTime12

# 格式化日期时间
# type: string
datetime=formatDateTime

# 获取时间上下文（包含多种时间信息）
# type: string
time_context=getTimeContext

# 获取系统信息
# type: string
system_info=getSystemInfo

# 获取 Node.js 版本
# type: string
node_version=getNodeVersion

# 获取操作系统平台
# type: string
platform=getPlatform

# 获取主机名
# type: string
hostname=getHostname

# 获取用户名
# type: string
username=getUsername

# 获取 CPU 核心数
# type: string
cpu_count=getCPUCount

# 获取总内存大小
# type: string
total_memory=getTotalMemory

# 获取可用内存大小
# type: string
free_memory=getFreeMemory

# 获取 GPU 信息
# type: string
gpu_info=getGPUInfo

# 获取 GPU 数量
# type: string
gpu_count=getGPUCount

# 获取 CPU 型号
# type: string
cpu_model=getCPUModel

# 获取 CPU 负载
# type: string
cpu_load=getCPULoad

# 获取详细内存信息
# type: string
memory_detail=getMemoryDetail

# 获取磁盘信息
# type: string
disk_info=getDiskInfo

# 获取操作系统版本
# type: string
os_version=getOSVersion

# 获取网络摘要信息
# type: string
network_summary=getNetworkSummary

# 获取系统摘要信息
# type: string
system_summary=getSystemSummary

# 获取完整系统 JSON 信息
# type: string
full_system_json=getFullSystemJSON

# 生成 UUID
# type: string
uuid=generateUUID

# 生成随机字符串
# type: string
random_string=generateRandomString

# 生成随机数字
# type: string
random_number=generateRandomNumber

# 生成随机整数
# type: string
random_int=randomInt

# 生成随机浮点数
# type: string
random_float=randomFloat

# 应用名称
# type: string
app_name=Sentra

# 应用版本
# type: string
app_version=1.0.0

# 获取 MCP 工具列表
# type: string
MCP_TOOLS=getMcpTools

# 获取微信系统提示词
# type: string
wechat_system_prompt=getWeChatSystemPrompt

# 获取沙盒系统提示词
# type: string
sandbox_system_prompt=getSandboxSystemPrompt

# 获取 QQ 系统提示词
# type: string
qq_system_prompt=getQQSystemPrompt

# 获取 Sentra 工具反馈规则
# type: string
sentra_tools_rules=getSentraToolFeedbackPrompt