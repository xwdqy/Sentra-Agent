export const DELAY_RUNTIME_KIND = Object.freeze({
  dueTriggerJob: 'reply_action_delay_due_trigger',
  dueReplayPayload: 'reply_action_delay_replay'
} as const);

export const DELAY_RUNTIME_AI_NAME = Object.freeze({
  dueTrigger: 'delay_due_trigger'
} as const);
